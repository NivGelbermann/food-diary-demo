<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.nivgelbermann.fooddiarydemo">

    <!-- TableRetrieverServicevice -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <provider
            android:name=".data.sqlite_to_be_deprecated.AppProvider"
            android:authorities="com.nivgelbermann.fooddiarydemo.provider"
            android:exported="false"/>

        <activity
            android:name=".ui.add_edit.AddEditActivity"
            android:label="@string/title_activity_add_edit"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.MainActivity"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.nivgelbermann.fooddiarydemo.ui.MainActivity"/>
        </activity>
        <activity
            android:name=".ui.add_edit.CategoryChooserActivity"
            android:label="@string/title_activity_category_chooser"
            android:parentActivityName=".ui.add_edit.AddEditActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.nivgelbermann.fooddiarydemo.ui.add_edit.AddEditActivity"/>
        </activity>

        <service
            android:name=".services.TableRetrieverService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"/>
    </application>

</manifest>